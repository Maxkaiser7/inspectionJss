import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.max.inspectapp',
  appName: 'inspectionjss',
  webDir: 'INSPECTIONJSS'
};

export default config;
